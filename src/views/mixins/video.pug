mixin video(video)
  div
    h4 
      a(href=`/video/${video.id}`)=video.title
    video(src="/"+video.fileUrl width="400px" style="object-fit: cover")
    p=video.description
    ul 
      each tag in video.hashtags
        li=tag
    small=video.createdAt
    hr
    
    

